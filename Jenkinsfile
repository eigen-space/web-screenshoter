// Powered by Infostretch 

timestamps {

node () {

	stage ('ams.web-screenshoter - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'derlag', url: 'https://github.com/cybernated/web-screenshoter.git']]]) 
	}
	stage ('ams.web-screenshoter - Build') {
 	
// Unable to convert a build step referring to "jenkins.plugins.publish__over__ssh.BapSshBuilderPlugin". Please verify and convert manually if required.
// Unable to convert a build step referring to "jenkins.plugins.publish__over__ssh.BapSshBuilderPlugin". Please verify and convert manually if required. 
	}
}
}